// GymboxProto — Swift bindings for the gymbox wire format.
//
// The generated `gymbox.pb.swift` file is produced by `scripts/gen_swift.sh`
// (protoc + protoc-gen-swift) and is NOT checked in. Run codegen first.
//
// This file exists so the target compiles before codegen and to expose the
// package/version constants.

import Foundation

public enum GymboxProto {
    public static let version = "0.1.0"
    public static let protoPackage = "gymbox.v1"
}
